// The Swift Programming Language
// https://docs.swift.org/swift-book

@main
struct SwiftPlayground {
    static func main() {
        let x = 15
        let y = 2

        let added = x + y
        let subtracted = x - y
        let multiplied = x * y
        let divided =  x / y
        let remainder = x % y

        let added2 = y + x
        let subtracted2 = y - x
        let multiplied2 = y * x
        let divided2 =  y / x
        let remainder2 = y % x

        print("\(y)")
    }
}
